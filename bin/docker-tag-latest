#!/usr/bin/env bash
set -Eeuo pipefail

# Tag latest release.
# Modified 2019-06-27.

repo="acidgenomics/$1"
tag="${2:-}"

if [[ -z "$tag" ]]
then
    tag="bioc-3.9"
fi

docker login
docker pull "${repo}:${tag}"
docker tag "${repo}:${tag}" "${repo}:latest"
docker push "${repo}:latest"

