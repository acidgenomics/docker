#!/usr/bin/env bash
set -Eeu -o pipefail

# Tag latest release.
# Modified 2019-09-18.

repo="acidgenomics/$1"
tag="${2:-}"

if [[ -z "$tag" ]]
then
    tag="bioc-release"
fi

docker login
docker pull "${repo}:${tag}"
docker tag "${repo}:${tag}" "${repo}:latest"
docker push "${repo}:latest"

